<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
    <title>Lista de Vehículos</title>
</head>
<body>
    <div class="container">
        <h1 class="my-4">Vehículos</h1>
        
        #for(vehiculo in vehicles):
            <div class="card mb-4">
                <div class="card-body">
                    <h2 class="card-title"><a href="#(vehiculo.id)">#(vehiculo.marca) #(vehiculo.modelo)</a></h2>
                </div>
            </div>
        #endfor
    </div>

    
</body>
</html>
